{
  "version": 2,
  "//regions": "Run the API in Mumbai, next to the database. Without this Vercel defaults to US East, so every query would cross the Pacific and back — the database's own region would count for nothing.",
  "regions": ["bom1"],
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "prisma/schema.prisma",
          "node_modules/.prisma/client/**",
          "node_modules/@prisma/client/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
